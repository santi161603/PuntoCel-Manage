<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Event" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->   <!-- Customize ActionBar -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- Custom ActionBar style -->
    <style name="MyActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/purple_500</item> <!-- Color de fondo del ActionBar -->
        <item name="titleTextStyle">@style/MyActionBarTitleText</item> <!-- Estilo del texto del título -->
    </style>

    <!-- Custom ActionBar title text style -->
    <style name="MyActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item> <!-- Color del texto del título -->
        <item name="android:textSize">20sp</item> <!-- Tamaño de texto -->
    </style>
</resources>